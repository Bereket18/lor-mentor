# Shared secret required from callers as the `x-verifier-token` header.
# Set the SAME value here and as RECEIPT_VERIFIER_TOKEN in the NestJS API .env.
# Leave blank to disable auth (NOT recommended outside local dev).
VERIFIER_SHARED_TOKEN=
